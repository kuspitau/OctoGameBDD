# P4-T01 — spell/recipe source and identity contract

**Status: VALIDATED**

## Goal

Establish the first bounded P4 contract before broad crafting ingestion: determine, from primary
sources, how OctoGameDB should identify and relate spells and recipes without conflating three
different concepts that often share user-facing names:

- the native spell learned/known by the character;
- the crafting/recipe spell that performs a craft;
- an item that teaches/unlocks that spell;
- the item produced by the craft.

The task must also establish where profession/skill-line membership and rank/skill requirements come
from, and which source surfaces are authoritative/complete versus positive/partial evidence.

P4-T01 is primarily a **source/identity-contract task**. Its purpose is to make the later recipe,
reagent and acquisition schema safe to implement.

## Preconditions

- P0 through P3-T05 are `VALIDATED`.
- The cumulative local canonical DB is `data/generated/octogamedb.sqlite3`, validated through
  migration 10 / P3-T05.
- D-029 / `docs/project/CANONICAL_DB.md` governs any future canonical DB mutation.
- Existing project rules requiring native IDs, explicit domain relations, provenance and
  recipe/spell/item distinctions remain mandatory.

No P4 task may infer that a recipe item ID, crafting spell ID and crafted item ID are interchangeable.

## Required investigation

Before designing a P4 canonical migration, inspect the relevant source behavior rather than guessing
from field names.

At minimum evaluate the useful/current surfaces that are actually available to the project, such as:

- public WoW/Vanilla DBC structures relevant to spell and skill-line identity;
- the pinned/current Tortoise source tree and world SQL where it carries recipe/trainer/item-teaching
  facts;
- CMaNGOS Vanilla schemas/data as a baseline where useful;
- pfQuest/pfQuest-turtle only for the fields they genuinely expose;
- reviewed OctoDB/Octo-specific pages only when they expose structurally meaningful P4 facts;
- the configured local OctoWoW client/addons only when public/source inspection cannot establish the
  required version-specific behavior.

Do not add a local path solely to understand a publicly inspectable format. If a real local path is
needed for Level 2 and is not already configured, follow `LOCAL_PATHS.md` and provide a safe
`get_path.bat`.

Record source revisions/versions whenever they affect parser or semantic behavior.

## Questions P4-T01 must answer

### Spell identity

- What is the stable native ID used by the relevant sources for spells?
- Which source provides a trustworthy spell name/localization for the project's default query model?
- Are ranks separate spell IDs, and if so must rank membership be modeled explicitly or deferred?
- Which source is sufficiently complete to establish spell presence versus positive-only observations?

### Recipe identity

- Is the durable recipe entity best keyed by the crafting/learned spell ID, another native ID, or an
  explicit relation over multiple native entities?
- How should the model represent a recipe that has no teaching item?
- How should the model represent multiple teaching items/sources for one crafting spell?
- Can one teaching item unlock more than one spell in any relevant source shape?
- Can a crafting spell produce multiple result items or variable quantities in the target data?

Do not choose a recipe primary key merely because a website page uses the word “recipe”.

### Profession/skill requirements

- Which source expresses profession/skill-line membership?
- Which field is the required profession skill value versus character level, spell rank or trainer
  requirement?
- Are source values complete enough to support absence/deletion semantics?
- How should profession/skill-line native identity be represented for later P4 tasks?

### Teaching/acquisition distinction

P4-T01 must explicitly distinguish:

```text
recipe/crafting spell
teaching item
trainer/NPC teaching source
quest teaching/reward source
crafted result item
```

Only the identities/relations proven in this task should be committed to subsequent P4 schema work.

## Out of scope

Keep these out of P4-T01 unless they are inseparable from proving identity semantics:

- complete reagent ingestion;
- recipe economics/profitability;
- trainer/vendor/quest acquisition graph implementation;
- full profession progression planning;
- spell effects unrelated to crafting identity;
- combat spell modeling;
- broad item-stat ingestion;
- P6-scale full import/performance work;
- graphical UI.

## Closeout — reviewed primary evidence

P4-T01 was implemented against GitHub `main` commit:

```text
7ed632296ffdfbb6ee677d1a10812579c6b90b1c
```

The key P4 semantic reference is the already-pinned Tortoise source:

```text
repository: https://github.com/Penqle/tortoise-wow
revision:   61a8269151721f6467eddb05e7bed37704d0fc0b
branch:     main
```

Reviewed files and semantics:

```text
src/game/Database/DBCStructure.h
  SkillLineAbilityEntry.id
  SkillLineAbilityEntry.skillId
  SkillLineAbilityEntry.spellId
  SkillLineAbilityEntry.req_skill_value
  SkillLineAbilityEntry.forward_spellid
  SkillLineAbilityEntry.min_value / max_value

src/game/Spells/SpellEntry.h
  SpellEntry.Id
  SpellEntry.SpellName
  SpellEntry.Rank
  SpellEntry.Effect[slot]
  SpellEntry.EffectBasePoints[slot]
  SpellEntry.EffectDieSides[slot]
  SpellEntry.EffectItemType[slot]
  SpellEntry.EffectTriggerSpell[slot]

src/game/Spells/SpellDefines.h
  SPELL_EFFECT_CREATE_ITEM = 24
  SPELL_EFFECT_LEARN_SPELL = 36
  SPELL_EFFECT_SKILL_STEP  = 44
  SPELL_EFFECT_TRADE_SKILL = 47

src/game/Spells/SpellEffects.cpp
  CREATE_ITEM consumes EffectItemType for the effect slot
  created quantity is the calculated effect value, not a recipe-item field

src/game/Objects/ItemPrototype.h
  item spell slots preserve SpellId / SpellTrigger / charges and are an array

sql/base/tw_world_npc_trainer.sql
sql/base/tw_world_npc_trainer_template.sql
src/game/Objects/Creature.h
src/game/Handlers/NPCHandler.cpp
  trainer rows preserve acquisition spell plus reqskill / reqskillvalue / reqlevel
  LEARN_SPELL trigger targets must be inspected before equating that acquisition spell with a recipe
```

The CMaNGOS Classic source already pinned in P1-T02 remains a compatible Vanilla DBC/parser semantic
baseline:

```text
cmangos/mangos-classic
revision: 9b682be617ac61c127c23aa60d7b4ffbc0ce37e6
```

No Octo-specific source was needed to establish these identity distinctions. Octo client DBC remains
the preferred later direct observation surface for actual Octo spell/skill-line presence and names,
when P4-T02 moves from contract to canonical ingestion.

## Closeout — established identity contract

D-034 records the durable contract. In summary:

1. `Spell.Id` is the stable native spell identity. Rank text is display/rank metadata on a spell row;
   do not merge rows merely because names match.
2. A P4 recipe identity is anchored to the **crafting spell native ID** once source evidence proves
   that the spell belongs to a profession/skill line and has at least one `CREATE_ITEM` effect. Recipe
   and Spell remain separate canonical entity types even when the recipe's durable native key is the
   crafting spell ID.
3. A teaching item is an acquisition source. Its item spell slot points to an acquisition spell; only
   a proven `LEARN_SPELL` effect may resolve that acquisition spell to the learned/crafting spell.
4. A trainer row likewise points to an acquisition spell and carries trainer-side requirements. Those
   fields are not the recipe's profession requirement unless independent skill-line evidence agrees.
5. `SkillLineAbility.skillId -> spellId` is the source relation for profession/skill-line membership.
   `req_skill_value` is the trade-skill requirement. It is distinct from trainer `reqskill`,
   `reqskillvalue`, `reqlevel`, spell rank text, and character level.
6. `CREATE_ITEM` and `LEARN_SPELL` are effect-slot relations. Slot/order must be preserved. The source
   shape permits multiple effects/outputs, and create quantity comes from calculated spell-effect
   semantics; later P4 code must not force a one-output or fixed-quantity assumption before source
   evidence supports it.
7. An item has multiple spell slots structurally. P4 therefore preserves item spell slot/order and
   must not assume one teaching item maps to exactly one spell merely from schema shape. This bounded
   task did not claim that every structurally possible multi-spell recipe item occurs in production.
8. Recipes with no teaching item are normal: trainer/quest/known-by-default/other acquisition can be
   represented independently of recipe identity.
9. Absence semantics are source-specific. Direct complete DBC rows can support bounded spell/
   skill-line membership claims for that exact client/source revision; item/trainer/world SQL and
   website/addon observations are not promoted to universal negative evidence without an explicit
   completeness contract.

## Tracked proof artifact

The reduced fixture:

```text
tests/fixtures/p4_t01/source_contract.json
```

is source-shaped around the exact relevant native field names. It deliberately contains:

- two craft spells in two different skill lines;
- one recipe with two `CREATE_ITEM` output slots;
- one recipe with no teaching item/trainer source;
- an item with more than one spell slot, only one of which resolves to the tested recipe;
- separate item-acquisition and trainer-acquisition `LEARN_SPELL` wrapper spells;
- deliberately different recipe skill requirement and trainer requirement values.

`src/octogamedb/importers/recipe_source_contract.py` normalizes only those established fields and
fails closed on unsupported contract shape, missing CREATE_ITEM target, duplicate slots/IDs or
dangling referenced spells. It does not mutate SQLite and is not yet the P4 canonical importer.

Deterministic tracked-fixture content hash at closeout:

```text
sha256:cf4661faa4e9f8f7ba7d4f38f2dea1175a02eb4f8236638b7b3704da9b59cf14
```

## Validation

Level 1 performed in the coding workspace:

```text
PYTHONPATH=src python -m pytest -q tests/test_recipe_source_contract.py
# 7 passed

PYTHONPATH=src python scripts/validate_p4_t01_contract.py
# status=ok; recipe_ids=[1000, 1100]; two professions; preserved output/item/trainer slots

PYTHONPATH=src python -m compileall -q \
  src/octogamedb/importers/recipe_source_contract.py \
  scripts/validate_p4_t01_contract.py \
  tests/test_recipe_source_contract.py
# success
```

Ruff was not installed in the coding environment, so the normal repository lint command was not
available there. Human local integration validation was subsequently completed successfully on
2026-08-26. The combined checks were:

```text
python -m pip install -e ".[dev]"                         PASS
pytest --basetemp=.pytest_tmp                            PASS
python -m ruff check src tests                           PASS
python -m compileall -q src tests                        PASS
python -m ruff check scripts                             PASS
python -m compileall -q scripts                          PASS
python scripts/validate_p4_t01_contract.py               PASS
```

The deterministic validator confirmed:

```text
status=ok
content_hash=sha256:cf4661faa4e9f8f7ba7d4f38f2dea1175a02eb4f8236638b7b3704da9b59cf14
recipe_count=2
recipe_ids=[1000, 1100]
```

The local validation wrapper also hashed the protected generated databases before and after the
remaining gate. Both remained byte-identical:

```text
data/generated/octogamedb.sqlite3
sha256:9c637ab40c2c5e3c2843e6c7d52fb5c75bbe05f57d05e2ea4d48ae7bd03b127d

data/generated/octogamedb_bak.sqlite3
sha256:3dc2a49092d108a1274e55e3052b3ba74711b5ec0f675c9ff2a201c287617443
```

No source-specific Level-2/full-data evidence was required to establish P4-T01's identity semantics:
relevant formats and behavior were inspectable in pinned primary public source, and the tracked proof
is intentionally source-shaped. No local path contract changed and no canonical DB was mutated.

## Completion condition

P4-T01 is `VALIDATED`. There is no migration and the canonical local database remains at migration 10.
The next bounded task is:

```text
P4-T02 — canonical spell / skill-line / recipe identity slice
```

P4-T02 must implement the first canonical schema/import slice from D-034 before broad reagent or
acquisition-graph ingestion.
